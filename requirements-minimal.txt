# Core requirements - always needed
click>=8.1.0
rich>=13.0.0
psutil>=5.9.0
pyyaml>=6.0
jinja2>=3.1.0
pefile>=2023.2.7
watchdog>=3.0.0
requests>=2.31.0
networkx>=3.0
colorama>=0.4.6
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0

# Platform-specific dependencies
# Windows
python-magic-bin>=0.4.14; platform_system == "Windows"
pythonnet>=3.0.0; platform_system == "Windows"

# Linux/Mac
python-magic>=0.4.27; platform_system != "Windows"

# Optional but recommended
# These enhance functionality but the tool works without them
matplotlib>=3.7.0
yara-python>=4.3.0; platform_system != "Windows" or platform_machine == "AMD64" or platform_machine == "x86_64"
docker>=6.0.0
scapy>=2.5.0
